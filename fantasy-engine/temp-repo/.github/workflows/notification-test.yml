name: Notification Test

on:
  workflow_dispatch: # Manual trigger only

jobs:
  test-notifications:
    runs-on: ubuntu-latest
    
    steps:
    - name: Test Success Notification
      run: |
        echo "🎉 This is a test to verify GitHub notifications are working!"
        echo "If you receive an email about this workflow, notifications are configured correctly."
        
    - name: Generate Test Report
      run: |
        echo "📧 Notification Test Report" > test-report.md
        echo "Timestamp: $(date)" >> test-report.md
        echo "Status: Success - Notifications should be working!" >> test-report.md
        
    - name: Upload Test Report
      uses: actions/upload-artifact@v4
      with:
        name: notification-test-report
        path: test-report.md